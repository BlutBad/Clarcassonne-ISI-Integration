<head>
	<title>Plataforma LL</title>
</head>

<body>
	<div id="div-login-button">{{loginButtons}}</div>
	<h1 id="titulo" ><FONT Size="7">Plataforma LL</FONT></h1>
	<p> Welcome to LowerLayers Games! </p>
   	<div id="container">
   		{{> cointemp}}
		{{> tabstemp}}
		{{> friendsTemp}}
   	</div>
</body>


<template name="cointemp">
	<div id="slider">
		<div id="coin-slider">
		    <IMG  SRC="frootwar.png">
		    <IMG  SRC="carcassonne.jpg">
		    <IMG  SRC="alieninvasion.png">
		</div>
	</div>
</template>

<template name="tabstemp">
	<div id="tabs" >
		<ul>
			<li><a href="#amigos">Amigos</a></li>
			<li><a href="#chat">Chat</a></li>
			<li><a href="#juegos">Juegos</a></li>
		</ul>
		
		<div id="amigos"> {{> userstemp}} </div>
		<div id="chat"> {{> loguserstemp}}{{> input}}{{> messagestemp}} </div>
    	<div id="juegos">{{> gamestemp}} {{> matchestemp}}</div>
	</div>
</template>

<template name="friendsTemp">
	
		
		<div id="accordion">
			<h3>Amigos</h3>
				<div>
					<p>lista amigos</p>
				</div>
			<h3>Buscar</h3>
				<div>
					<p>Buscar</p>
				</div>
		</div>
		<a id="friends" class="escondido" style="cursor:pointer;" >
			Amigos
		</a>
	
</template>

<template name="userstemp">
	<div id="users" >
		{{# each users}}
				<p>{{username}}</p>
		{{/each}}
	</div>
</template>

<template name="input">
	<p> Mensaje: <input type="text" id="message"></p>
</template>

<template name="messagestemp">
	<div id="messages">
		{{# each messages}}
			<i>{{name}}: {{message}}</i></br>
		{{/each}}
	</div>
</template>

<template name="loguserstemp">
	<div id="logusers">
		{{# each logusers}}
			{{username}}
		{{/ each}}
	</div>
</template>

<template name="gamestemp">
	<div id="games">
		{{# each games}}
			<a href="#" class="linkgame">{{name}}</a>
		{{/ each}}
	</div>

</template>

<template name="matchestemp">
	<div id="matches">
		<table>
			{{# each matches}}
				<tr>
					<td><a href="#">{{name}}</a></td><td> - {{created}}</td>
				</tr>
			{{/ each}}
		</table>
		<p> Nueva partida: <input type="text" id="match_creator"></p>
		<p><a href="#">Atrás</a></p>
	</div>
</template>
